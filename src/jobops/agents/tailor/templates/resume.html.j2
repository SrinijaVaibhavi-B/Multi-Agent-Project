<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<style>
  @page { margin: 4px; }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    font-family: "Calibri", "Trebuchet MS", "Arial", sans-serif;
    font-size: 9pt;
    color: #1f2937;
    background: #ffffff;
    padding: 4px 6px;
    line-height: 1.3;
  }

  /* ── Header ── */
  .header {
    background: #f0f9ff;
    border-left: 5px solid #38bdf8;
    padding: 9px 14px 8px;
    margin-bottom: 6px;
  }
  .header-inner {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
  }
  .name {
    font-size: 20pt;
    font-weight: 700;
    color: #0c4a6e;
    letter-spacing: 1.5px;
    line-height: 1;
    text-transform: uppercase;
  }
  .name-sub {
    font-size: 8pt;
    color: #0284c7;
    letter-spacing: 0.8px;
    margin-top: 3px;
    font-weight: 600;
  }
  .contact-block {
    text-align: right;
    font-size: 7.5pt;
    color: #475569;
    line-height: 1.7;
  }
  .contact-block a { color: #0284c7; text-decoration: none; }

  /* ── Section titles ── */
  .section-title {
    font-size: 8pt;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1.6px;
    color: #0284c7;
    border-bottom: 1.5px solid #bae6fd;
    margin-top: 7px;
    margin-bottom: 3px;
    padding-bottom: 1px;
  }

  /* ── Summary ── */
  .summary {
    font-size: 8.5pt;
    line-height: 1.45;
    color: #334155;
    padding: 3px 0;
  }

  /* ── Experience ── */
  .job { margin-bottom: 5px; }

  .job-header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 0px;
  }
  .job-company {
    font-weight: 700;
    font-size: 9.5pt;
    color: #0c4a6e;
  }
  .job-dates {
    font-size: 7.5pt;
    font-weight: 700;
    color: #0284c7;
    background: #e0f2fe;
    padding: 1px 7px;
    border-radius: 10px;
  }
  .job-sub {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 2px;
  }
  .job-title {
    font-style: italic;
    font-size: 8.5pt;
    color: #475569;
  }
  .job-location {
    font-size: 7.5pt;
    color: #94a3b8;
  }
  .job-contract {
    font-size: 7.5pt;
    color: #94a3b8;
    font-style: italic;
    margin-bottom: 2px;
  }

  ul { padding-left: 11px; }
  li {
    margin-bottom: 2px;
    font-size: 8.5pt;
    line-height: 1.35;
    color: #334155;
  }
  li strong {
    font-weight: 700;
    color: #0c4a6e;
  }

  /* ── Skills ── */
  .skills-table { width: 100%; border-collapse: collapse; }
  .skill-label {
    font-weight: 700;
    font-size: 7.5pt;
    color: #0284c7;
    width: 92px;
    vertical-align: top;
    padding: 1px 5px 1px 0;
    white-space: nowrap;
  }
  .skill-value {
    font-size: 8pt;
    color: #334155;
    padding: 1px 0;
    border-bottom: 1px solid #f0f9ff;
  }

  /* ── Education ── */
  .edu-header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
  }
  .edu-school { font-weight: 700; font-size: 9.5pt; color: #0c4a6e; }
  .edu-sub {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 1px;
  }
  .edu-degree { font-style: italic; font-size: 8.5pt; color: #475569; }
  .edu-dates {
    font-size: 7.5pt;
    font-weight: 700;
    color: #0284c7;
    background: #e0f2fe;
    padding: 1px 7px;
    border-radius: 10px;
  }
  .edu-location { font-size: 7.5pt; color: #94a3b8; }

  /* ── Projects ── */
  .proj-header { margin-bottom: 1px; }
  .project-name { font-weight: 700; font-size: 9pt; color: #0c4a6e; }
  .project-stack { font-size: 8pt; color: #0284c7; font-style: italic; }

  /* ── Certifications ── */
  .cert-line { font-size: 8pt; color: #334155; margin-top: 2px; }
  .cert-line a { color: #0284c7; text-decoration: none; font-weight: 600; }
</style>
</head>
<body>

<!-- HEADER -->
<div class="header">
  <div class="header-inner">
    <div>
      <div class="name">{{ name }}</div>
      <div class="name-sub">Software Engineer &nbsp;·&nbsp; AI &amp; Agentic Systems &nbsp;·&nbsp; Full-Stack &nbsp;·&nbsp; Cloud</div>
    </div>
    <div class="contact-block">
      <a href="mailto:{{ email }}">{{ email }}</a><br>
      {{ phone }}<br>
      <a href="{{ github }}">{{ github | replace("https://github.com/", "github.com/") }}</a><br>
      <a href="{{ linkedin }}">{{ linkedin | replace("https://www.linkedin.com/in/", "linkedin.com/in/") }}</a>
    </div>
  </div>
</div>

<!-- SUMMARY -->
{% if summary %}
<div class="section-title">Summary</div>
<div class="summary">{{ summary }}</div>
{% endif %}

<!-- SKILLS -->
<div class="section-title">Skills</div>
<table class="skills-table">
{% for label, items in skills.items() %}
<tr>
  <td class="skill-label">{{ label }}</td>
  <td class="skill-value">{{ items | join(", ") }}</td>
</tr>
{% endfor %}
</table>

<!-- EXPERIENCE -->
<div class="section-title">Experience</div>
{% for job in experience %}
<div class="job">
  <div class="job-header">
    <span class="job-company">{{ job.company }}</span>
    <span class="job-dates">{{ job.dates }}</span>
  </div>
  <div class="job-sub">
    <span class="job-title">{{ job.title }}</span>
    <span class="job-location">{{ job.location }}</span>
  </div>
  {% if job.contract_notes is defined and job.contract_notes %}
  <div class="job-contract">{{ job.contract_notes }}</div>
  {% endif %}
  <ul>
  {% for bullet in job.bullets %}
    <li>{{ bullet }}</li>
  {% endfor %}
  </ul>
</div>
{% endfor %}

<!-- VOLUNTEER -->
{% if volunteer %}
<div class="section-title">Volunteer</div>
{% for v in volunteer %}
<div class="job">
  <div class="job-header">
    <span class="job-company">{{ v.org }}</span>
    <span class="job-dates">{{ v.dates }}</span>
  </div>
  <div class="job-sub">
    <span class="job-title">{{ v.title }}</span>
    <span class="job-location">{{ v.location }}</span>
  </div>
  <ul>
  {% for bullet in v.bullets %}
    <li>{{ bullet }}</li>
  {% endfor %}
  </ul>
</div>
{% endfor %}
{% endif %}

<!-- EDUCATION -->
<div class="section-title">Education</div>
{% for edu in education %}
<div style="margin-bottom:3px;">
  <div class="edu-header">
    <span class="edu-school">{{ edu.school }}</span>
    <span class="edu-location">{{ edu.location }}</span>
  </div>
  <div class="edu-sub">
    <span class="edu-degree">{{ edu.degree }}{% if edu.gpa %} &nbsp;·&nbsp; GPA: {{ edu.gpa }}{% endif %}</span>
    <span class="edu-dates">{{ edu.dates }}</span>
  </div>
  {% if edu.notes %}
  <ul style="margin-top:1px;">
  {% for note in edu.notes %}<li>{{ note }}</li>{% endfor %}
  </ul>
  {% endif %}
</div>
{% endfor %}

<!-- PROJECTS -->
{% if projects %}
<div class="section-title">Projects</div>
{% for proj in projects %}
<div class="proj-header">
  <span class="project-name">{{ proj.name }}</span>
  {% if proj.stack %}<span class="project-stack"> &nbsp;|&nbsp; {{ proj.stack }}</span>{% endif %}
</div>
<ul style="margin-bottom:4px;">
{% for bullet in proj.bullets %}<li>{{ bullet }}</li>{% endfor %}
</ul>
{% endfor %}
{% endif %}

<!-- CERTIFICATIONS -->
{% if certifications %}
<div class="section-title">Certifications</div>
<div class="cert-line">
{% for cert in certifications %}{% set name = cert.name if cert is mapping else cert %}{% set url = cert.url if cert is mapping else None %}{% if url %}<a href="{{ url }}">{{ name }}</a>{% else %}{{ name }}{% endif %}{% if not loop.last %} &nbsp;|&nbsp; {% endif %}{% endfor %}
</div>
{% endif %}

<!-- WORK AUTHORIZATION -->
{% if work_authorization %}
<div style="margin-top:6px; font-size:7.5pt; color:#6b7280; border-top:1px solid #e5e7eb; padding-top:4px;">
  <strong style="color:#374151;">Work Authorization:</strong> {{ work_authorization }}
</div>
{% endif %}

<!-- ATS KEYWORD INJECTION — invisible to human readers -->
{% if jd_raw %}
<div style="font-size:5pt; color:#ffffff; background:#ffffff; line-height:1; margin-top:2px; overflow:hidden;">{{ jd_raw }}</div>
{% endif %}

</body>
</html>
